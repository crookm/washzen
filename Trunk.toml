[build]
target = "index.html"
release = false
dist = "dist"
public_url = "/"
filehash = true
inject_scripts = true
frozen = false
locked = false
minify = "on_release" # can be one of: never, on_release, always

[watch]
watch = []
ignore = []

[serve]
addresses = ["127.0.0.1"]
port = 3000
